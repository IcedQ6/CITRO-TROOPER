using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WaveManager : MonoBehaviour
{
    // An array of each enemy in the wave
    
    // First value holds the index with a delay before it spawns,
    // Second value is the length of that delay.
    public Vector2[] delayList;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
